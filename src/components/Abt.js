import React from 'react';


const ResumeTemplate = ({ template, onEdit, onDelete, onView }) => {
  return (
    <div className="template">
     {{Footer}}
    </div>
  );
};

export default ResumeTemplate;
